# v0.1 Parallel Development Plan

- Status: Active
- Last updated: 2026-06-25
- Scope: Durable coordination notes for parallel v0.1 implementation work.
- Evidence: `docs/plans/v0.1-parallel-development-plan.md`, `docs/roadmap.md`
- Related canonical docs: `AGENTS.md`, `docs/development/agent-workflow.md`
- Supersedes: None
- Superseded by: None

## Durable knowledge

- Current HEAD already has repo-local lifecycle, TS/JS discovery, syntax-only
  code-unit extraction, generation-scoped SQLite activation,
  active `files`/`units` reads for syntax-only inventory, semantic-fact/evidence
  storage substrate, and Rust-side TypeScript semantic-worker process/NDJSON
  validation.
- Do not assign agents to build storage from scratch or add first indexing;
  those slices exist and should be extended through canonical specs.
- Parallel implementation should split by boundary: parser/IR, TypeScript
  compiler worker source, command-level semantic-fact indexing, storage read path,
  family-query contracts, MCP transport, installer writes, docs/tests.
- Keep ownership disjoint by path and canonical document. Integrate only after
  reviewing logic and running the required gates.
- Major features need a dedicated branch and atomic commits with tests/docs.

## Revalidation conditions

Update after Tree-sitter integration, TypeScript compiler worker source,
command-level semantic-fact indexing, family-query read paths, MCP serving,
installer writes, or any phase-scope change.
