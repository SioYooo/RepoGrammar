# v0.1 Substrate Hardening Checkpoint

- Status: Active
- Last updated: 2026-06-25
- Scope: Durable coordination notes for the hardening checkpoint before new
  v0.1 product feature work.
- Evidence: `docs/plans/v0.1-substrate-hardening-checkpoint.md`,
  `docs/plans/v0.1-parallel-development-plan.md`, `docs/roadmap.md`
- Related canonical docs: `docs/README.md`, `docs/specifications/storage.md`,
  `docs/specifications/indexing-pipeline.md`,
  `docs/specifications/cli.md`,
  `docs/specifications/semantic-workers.md`
- Supersedes: None
- Superseded by: None

## Durable knowledge

- The next v0.1 step is substrate hardening, not family mining, MCP serving,
  installer writes, Python, CodeGraph runtime integration, or Tree-sitter
  replacement.
- The hardening checkpoint is anchored to a source-backed audit of
  `c078a82cdfefbdb436adb6df80ac871bd6be615a`, but agents must re-inspect
  current HEAD before editing because later commits may already fix or change
  individual items.
- P1 hardening covers generation immutability, lifecycle doctor hygiene, and
  repo-guard coverage for current planning/specification/memory artifacts.
- P2 hardening covers bounded filesystem reads, parent Git ignore behavior,
  JSON-based bootstrap manifest validation, split manifest/storage schema
  reporting, semantic-worker request-size alignment, and the index-lock release
  gate.
- The index-lock release gate now has a narrow implementation: `index` and
  `sync` acquire `.repogrammar/locks/index.lock` before generation preparation,
  hold it through validation and activation, refuse live or unknown ownership,
  replace confirmed stale same-host locks during acquisition, clean up their own
  successful lock, and report lock state through `doctor`.
- Later v0.1 family work should resume with FamilyStore, lightweight TS/JS
  framework role facts, EC-MVFI-lite grouping, and evidence-backed
  `families`/`family`/`find`/`check`/`explain` output or typed `UNKNOWN`.
- The FamilyStore storage substrate now exists for generation-scoped family
  records, members, variation slots, and family-bound evidence. It is not yet
  populated by `index`/`sync`, and pattern-family CLI/MCP queries still require
  framework role facts plus EC-MVFI-lite claim builders before they can become
  implemented.

## Implications

Future agents should read
`docs/plans/v0.1-substrate-hardening-checkpoint.md` instead of carrying a long
chat prompt. Do not proceed to MCP or installer writes while P1 checkpoint
items remain open.

## Revalidation conditions

Update this memory after the hardening checkpoint lands, after an explicit
decision defers any checkpoint item, or before starting family evidence store,
EC-MVFI-lite, MCP, installer, Python, or CodeGraph runtime work.
