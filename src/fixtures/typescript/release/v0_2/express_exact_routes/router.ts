import { Router } from "express";

const router = Router();

router.put("/accounts/:id", (req, res) => {
  res.json({});
});

router.patch("/accounts/:id", (req, res) => {
  res.json({});
});

export { router };
