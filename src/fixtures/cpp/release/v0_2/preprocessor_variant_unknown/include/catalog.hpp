#ifndef CATALOG_H
#define CATALOG_H

// A standard include guard is NOT a build variant. The guarded body must never
// take a cpp_build_variant UNKNOWN just for being inside the guard region.

struct CatalogConfig
{
    int page_size;
};

#endif
