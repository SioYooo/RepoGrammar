# Incremental-equivalence oracle fixture

This mixed-language tree is copied into isolated workspaces by
`repo-guard sync-equivalence`. Each scenario applies one scripted patch and
compares the incremental-sync state against a clean full rebuild. Markdown is
not a discovered language, so editing this file is the docs-only no-op scenario.
