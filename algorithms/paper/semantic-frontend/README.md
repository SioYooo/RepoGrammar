# Semantic Frontend

The TypeScript worker must use compiler-native semantics behind a versioned
protocol. Unsupported compiler API versions fall back to syntax-only evidence
with `SEMANTIC_VERSION_UNSUPPORTED`.
