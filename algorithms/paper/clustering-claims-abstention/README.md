# Clustering, Claims, And Abstention

Family construction must use clustering as a seed/refinement mechanism, then
infer claims through source-backed atomic predicates. Low support, conflicts,
or incompatible targets remain `UNKNOWN`.
