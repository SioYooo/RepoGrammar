# Candidate Retrieval

Candidate retrieval finds possible analogues cheaply. It must never be treated
as final family membership; compatibility, semantics, evidence, and abstention
gates decide claims.
